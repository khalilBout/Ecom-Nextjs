import FormAddAddress from "@/utils/action/FormAddAddress";

const Address = ({ setAddressClient }) => {
  return (
    <>
      <FormAddAddress setAddressClient={setAddressClient} />
    </>
  );
};

export default Address;
